services:

  # A Docker web service
  - type: web
    name: cpatinore
    env: docker
    repo: https://github.com/cpatinore/website # optional
    plan: free
    branch: main
    rootDir: ./dockerfile